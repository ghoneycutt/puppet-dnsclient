language: ruby
before_script: "gem install --no-ri --no-rdoc bundler; git clone https://github.com/puppetlabs/puppetlabs-stdlib.git /home/travis/build/ghoneycutt/puppet-dnsclient/spec/fixtures/modules/stdlib/"
after_script:
script: "rake spec"
notifications:
  email: false
rvm:
  - 1.9.3
  - 1.8.7
env:
  - PUPPET_VERSION=2.7.13
  - PUPPET_VERSION=3.1.0
gemfile: Gemfile
branches:
  only:
    - rebase_fun
matrix:
  allow_failures:
    - rvm: 1.9.3
